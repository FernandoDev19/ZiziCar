model Gamma {
  id              String    @id @default(uuid())
  name            String    @unique
  image_url       String
  precio_promedio Decimal   @db.Decimal(10, 0)
  requests        Request[]

  @@map("ZIC_ADM_GAMMA")
}
